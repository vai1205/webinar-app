import React from 'react';
import Navbar from '../components/navbar';

const Home = props => {
    return(
        <React.Fragment>
            <h1>Home</h1>
            <Navbar/>
        </React.Fragment>
    );
};

export default Home;