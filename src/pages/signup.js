import React from 'react';
import SignupCard from '../components/signupCard';

const Signup = props => {
    return(
        <React.Fragment>
            <SignupCard/>
        </React.Fragment>
    );
};

export default Signup;